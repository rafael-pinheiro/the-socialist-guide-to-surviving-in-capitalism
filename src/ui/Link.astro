---
interface Props {
  href: string;
  title: string;
  rel?: string;
  class?: string;
}

const { href, title, rel, class: className } = Astro.props;
---

<a
  class={`${className} text-amber-500 hover:text-commie`}
  href={href}
  title={title}
  rel={rel}
>
  <slot />
</a>
