---
interface Props {
  href: string;
  title: string;
  rel?: string;
}

const { href, title, rel } = Astro.props;
---

<a class="text-amber-500 hover:text-commie" href={href} title={title} rel={rel}
  ><slot />
</a>
